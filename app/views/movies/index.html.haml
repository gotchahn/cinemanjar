
= render "movies_tab"

%hr
.row
  %h3
    %em
      Recommended Movies
      %span.text-small.text-muted Based on your favorite genres

-if @movies_recommended.any?
  = render partial: "movies/table", locals: { movies: @movies_recommended }
- else
  .row.justify-content-center
    %em
      %p.text-muted Start picking movies to build your favorite genres

%hr
.row
  %h3
    %em Other Movies

= render partial: "movies/table", locals: { movies: @other_movies }
